"""Utility methods for creating basic LangChain chains."""
from typing import Any, Dict, Optional

from langchain.chains import LLMChain


def chain(llm, llm_kwargs: Optional[Dict[str, Any]] = {}, memory=None):
    pass
